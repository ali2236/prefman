name: prefman
description: A better way to manage App Preferences using persistant variables in a Structured Way.
version: 1.1.0
homepage: https://github.com/ali2236/prefman

environment:
  sdk: ">=2.12.0 <4.0.0"
  flutter: ">=2.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter